slurm:
  name:
    - eval:sys.argv[0][:-3].split('/')[-1]
    - ${slurm.train.noise}
    - ${data.fdset}
    - orig
    - ${data.tdset}
    - ${train.seed}
    

eval:
  model:
    date: 2020-03-11
    name:
      - launch_train
      - ${slurm.train.noise}
      - ${data.fdset}
      - orig
      - ${train.seed}
    bin: orig
    data: anli
  data: anli

data:
  bin: orig
  num_sents: 17000

display:
  dir:
    date: 2020-03-24
    name:
      - eval_roberta_glue
      - ???
      - ???
      - orig
      - ???
      - ???

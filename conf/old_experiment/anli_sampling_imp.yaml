slurm:
  name:
    - eval:sys.argv[0][:-3].split('/')[-1]
    - ${slurm.train.noise}
    - ${data.fdset}
    - sampling_imp
    - ${data.tdset}
    - ${train.seed}
    - ${gen.shard}

gen:
  depth: 1
  num_samples: 5
  mask_prob: 0.15
  leave_unmasked_prob: 0.1
  random_token_prob: 0.1
  topk: -1
  shard: ~
  num_shards: ~
  label_samples: 1
  dset: sampling.imp
  model:
    date: 2020-03-11
    name: 
      - launch_train
      - ${slurm.train.noise}
      - ${data.fdset}
      - ${gen.model.bin}
      - ${gen.model.seed}
    bin: orig
    seed: 69
  batch: 8

eval:
  model:
    date: 2020-03-21
    name:
      - launch_train
      - ${slurm.train.noise}
      - ${data.fdset}
      - sampling_imp
      - ${train.seed}
    bin: sampling.imp
    data: anli
  data: anli

data:
  bin: sampling.imp
  num_sents: 150000

display:
  dir:
    date: 2020-03-24
    name:
      - eval_roberta_glue
      - ???
      - ???
      - sampling_imp
      - ???
      - ???

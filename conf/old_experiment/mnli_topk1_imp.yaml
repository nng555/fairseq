slurm:
  name:
    - eval:sys.argv[0][:-3].split('/')[-1]
    - ${slurm.train.noise}
    - ${data.fdset}
    - top1_imp
    - ${data.tdset}
    - ${gen.shard}
    - ${train.seed}

gen:
  depth: 1
  shard: ~
  num_shards: 8
  num_samples: 5
  label_samples: 1
  mask_prob: 0.15
  leave_unmasked_prob: 0.1
  random_token_prob: 0.1
  topk: 1
  dset: top1.imp
  model:
    date: 2020-02-19
    name: 
      - roberta_base_finetune
      - ${data.fdset}
      - ${gen.model.bin}
      - ${gen.model.seed}
    bin: orig
    seed: 69
  batch: 8

eval:
  model:
    date: 2020-03-21
    name:
      - launch_train
      - ${slurm.train.noise}
      - ${data.fdset}
      - top1_imp
      - ${train.seed}
    bin: top1.imp
    data: mnli
  data: mnli

data:
  bin: top1.imp
  num_sents: 500000

display:
  dir:
    date: 2020-03-24
    name:
      - eval_roberta_glue
      - ???
      - ???
      - top1_imp
      - ???
      - ???

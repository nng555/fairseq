slurm:
  name:
    - eval:sys.argv[0][:-3].split('/')[-1]
    - ${slurm.train.noise}
    - ${data.fdset}
    - sampling
    - ${data.tdset}

gen:
  depth: 1
  num_samples: 5
  mask_prob: 0.15
  leave_unmasked_prob: 0.1
  random_token_prob: 0.1
  topk: -1
  dset: bert_sampling
  model:
    date: 2020-02-19
    name: 
      - roberta_base_finetune
      - ${slurm.train.noise}
      - ${data.fdset}
      - ${gen.model.bin}
      - ${gen.model.seed}
    bin: orig
    seed: 69
  batch: 8

eval:
  date: 2020-02-24
  run_name:
    - launch_train
    - ${slurm.train.noise}
    - ${data.fdset}
    - sampling

data:
  bin: bert.sampling1
  num_sents: 500000

display:
  dir:
    date: 2020-02-25
    name:
      - eval_roberta_glue
      - ???
      - ???
      - sampling
      - ???
      - ???

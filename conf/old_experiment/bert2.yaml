slurm:
  name: 
    - roberta_base_finetune
    - ${train.noise}
    - ${data.fdset}
    - ${data.bin}
    - ${data.tdset}
    - ${train.seed}

eval:
  date: 2020-02-19
  run_name: 
    - roberta_base_finetune
    - ${train.noise}
    - ${data.fdset}
    - ${data.bin}
    - ${train.seed}

data:
  bin: bert2_noised_fix
